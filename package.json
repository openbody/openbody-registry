{
  "name": "@openbody/registry",
  "version": "0.0.1",
  "private": true,
  "description": "OpenBody canonical exercise registry (CC0): base movements + facets (SPEC §6).",
  "license": "CC0-1.0",
  "type": "module",
  "scripts": {
    "check": "node tools/validate.mjs",
    "crosswalk": "node tools/build-crosswalk.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
